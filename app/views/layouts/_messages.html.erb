<% flash.each do |msg_type, msg_content| %>
    <div class="ui message <%= msg_type%> transition">
        <i class="close icon"></i>
        <div class="header">
            <%= msg_content %>
        </div>
    </div>
<% end %>